services:
  - type: web # <-- для статического сайта используется type: web
    name: xtgameapp
    runtime: static # <-- обязательно для статики
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
    - key: VITE_API_BASE
      value: https://tyanrl-xtgame-0153.twc1.net/api/v1